---
'@grasdouble/slm_parcel_learn': patch
---

fix: replace `sitemap.xml` export with `sitemap-publishedAt-filter.json` to match the output of `sitemapPublishedAtFilterPlugin` and resolve the 502 on `/learn/sitemap.xml`.
