---
'@grasdouble/slm_parcel_learn': minor
---

feat: add `order` frontmatter field to control guide display order within catalogs — `learn.ts` now sorts `guideIds` by ascending `order`, all existing guides have been assigned an order value.
